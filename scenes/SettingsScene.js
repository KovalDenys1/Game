export default class SettingsScene extends Phaser.Scene {
    constructor() {
        super({ key: 'SettingsScene' });
    }

    preload() {
        // Загрузка ресурсов для сцены с боссами
    }

    create() {
        // Логика создания сцены с боссами
        console.log('Добро пожаловать на арену с боссами!');
    }

    update() {
        // Логика обновления сцены с боссами
    }
}
